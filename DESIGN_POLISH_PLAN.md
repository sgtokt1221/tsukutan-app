# つくたん UIデザイン・ポリッシュ実装計画

## 0. この文書の位置づけ

- 対象は、つくたんの生徒画面・学習画面・管理画面を横断するUI改善である。
- ブランドカラーの色相は変更しない。
- 機能仕様、学習ロジック、保存データ、パスワード方式は変更しない。
- 実装前の判断基準、作業順、画面別の完成像、受け入れ条件を定義する。
- 機能面の工程は `IMPLEMENTATION_PLAN.md` を正とし、本書はそのUI品質を補完する。

## 1. プロダクト仮説

**既存の親しみやすいライムと青を保ちながら、情報の優先順位・余白・操作状態を一貫させれば、生徒は迷わず今日の学習を始められ、管理者は必要な生徒を素早く判断できる。**

## 2. デザインの到達目標

1. 最初の3秒で「現在地」「次にすること」「進み具合」が分かる。
2. 生徒向けは親しみやすく、幼すぎない学習ツールにする。
3. 管理者向けは情報量を保ちつつ、視線移動と判断負荷を減らす。
4. 同じ役割の部品は、全画面で同じ見た目と反応を持つ。
5. スマートフォンでも主要操作を片手で完了できる。
6. 色だけに頼らず、形・文字・アイコンでも状態を判別できる。
7. 派手さより、整列・密度・コントラスト・反応の質で格好よくする。

## 3. 優先度付き所見

### P0-1: スタイルの責任範囲が重複している

- Issue: 共通クラス、画面固有CSS、JSX内の直接指定が同じ要素を上書きしている。
- Evidence: `dashboard-header` などが複数CSSに存在し、`StudentDashboard.js` にも色・余白・影・レスポンシブ判定が直接書かれている。
- Impact: 修正が別画面へ波及し、画面ごとに角丸や影の強さが揺れる。
- Fix: 基礎トークン、共通部品、画面固有スタイルの3層へ分離し、直接指定を段階的にクラスへ移す。

### P0-2: 主要アクションの優先順位が見た目から判断しにくい

- Issue: ライム、青、グレー、赤、橙のボタンが近い強さで並ぶ場面がある。
- Evidence: 学習開始、戻る、単語帳切替、テスト開始などで、背景色付きボタンが同時に表示される。
- Impact: 生徒が次に押すべき操作を迷い、誤操作も起きやすい。
- Fix: 1画面1プライマリーを原則とし、その他をセカンダリー、ゴースト、危険操作へ整理する。

### P0-3: 学習画面の情報量が主課題より目立つ

- Issue: テスト中の詳細説明、推定値、モード説明が単語カードより強く見える。
- Evidence: `VocabularyCheckTest.js` のヘッダーに複数色の状態文と長い説明が並ぶ。
- Impact: 回答へ集中しにくく、特にスマートフォンでカードが下へ押し出される。
- Fix: 常時表示を「ステージ、現在数、進捗」に絞り、詳細は折りたたみ領域へ移す。

### P0-4: モバイルで固定高さと固定配置が衝突しやすい

- Issue: `100vh`、固定ボタン、下部タブ、スクロール領域が混在する。
- Evidence: ダッシュボード、フラッシュカード、固定位置の単語帳ボタンに個別の高さ指定がある。
- Impact: 小さい端末やソフトウェアキーボード表示時に操作が隠れる。
- Fix: `100dvh` と安全領域を基準にし、固定操作は共通フッター領域へ統合する。

### P1-1: カードの強さが均一で、情報階層が平坦

- Issue: 多くのカードが同じ白背景、角丸、影、余白を持つ。
- Evidence: サマリー、今日のタスク、ストーリー、管理者詳細が似た外観で連続する。
- Impact: 重要なカードと補助情報の区別がつきにくい。
- Fix: 強調カード、標準カード、サブパネルの3段階へ限定する。

### P1-2: 文字サイズ・太さ・行間に一貫したスケールがない

- Issue: `rem`、`px`、インライン値が混在し、見出しの強さも画面ごとに異なる。
- Evidence: ロゴ、見出し、カードタイトル、メタ情報に多数の個別指定がある。
- Impact: 整列していても雑然と見え、長い日本語ラベルの折返しも不安定になる。
- Fix: 7段階の文字トークンと3段階の行間に統一する。

### P1-3: グレー系と角丸の種類が多い

- Issue: Slate、Gray、Bootstrap系のグレーと、4〜20pxの角丸が混在する。
- Evidence: `App.css` と `AdminDashboard.css` で複数体系の中間色が使われている。
- Impact: 同じプロダクト内で別製品の画面が連結した印象になる。
- Fix: オリーブ由来の文字色、白、淡い黄緑、透明境界を基本とし、角丸を4種へ集約する。

### P1-4: 管理画面のナビゲーションと詳細の視線経路が弱い

- Issue: 生徒一覧、分析、インポート、生徒登録が同一領域に密集する。
- Evidence: サイドバー見出し付近に複数ボタンが並び、選択中の生徒と画面モードの表現が競合する。
- Impact: 「誰を見ているか」と「何の作業中か」を見失いやすい。
- Fix: グローバル機能と生徒選択を分離し、選択行の左アクセントとページ見出しで現在地を固定する。

### P2-1: 絵文字とアイコンの視覚言語が混在する

- Issue: React Icons、絵文字、文字矢印が同時に使われる。
- Evidence: 結果画面の統計、テスト説明、戻る操作で表現方式が異なる。
- Impact: UIの精度が下がって見え、読み上げ時にも不要な情報となる。
- Fix: 操作アイコンは既存アイコンライブラリへ統一し、絵文字は祝福・学習の感情表現に限定する。

### P2-2: 動きのルールと停止条件が不統一

- Issue: hover浮上、bounce、カード回転などの時間と目的が揃っていない。
- Evidence: 画面ごとに0.18〜0.4秒の個別transitionがある。
- Impact: 動きが装飾として見え、操作結果との対応が弱くなる。
- Fix: 速い反応、標準遷移、学習カード遷移の3種類へ統一し、縮小モーション設定へ対応する。

## 4. デザイン原則

### 4.1 今日の一歩を最も強くする

- 最初の画面では「今日やること」を最上位に置く。
- プライマリー操作は同一ビューに原則1つとする。
- 説明より先に、行動名・所要時間・残量を見せる。

### 4.2 親しみやすさは色、格好よさは構造で作る

- ライムのブランド感は維持する。
- 影やグラデーションを増やさず、整列と余白で完成度を上げる。
- 角丸は柔らかさのために使い、すべてをピル型にはしない。

### 4.3 状態は必ず複数の手掛かりで伝える

- 選択中は色に加えて境界、チェック、太さを変える。
- エラーは色に加えてアイコン、見出し、具体的な対処を出す。
- 無効状態は薄くするだけでなく、押せない理由を隣接表示する。

### 4.4 学習中は集中、完了時は達成感

- 回答中は補助情報を抑える。
- 結果画面では数値、意味、次の行動の順に見せる。
- 紙吹雪のような過剰演出ではなく、短いスケール変化と進捗の伸長で達成感を作る。

### 4.5 管理画面は比較可能性を優先する

- カードの高さ、数値の桁位置、表の列を揃える。
- 注意対象は色だけでなく、理由と次の行動を同じ領域に置く。
- 破壊的操作は通常業務から空間的に離す。

## 5. 色相固定のカラートークン

### 5.1 変更禁止の基準色

```css
:root {
  --color-brand-lime: #A3E635;
  --color-canvas-lime: #F7FEE7;
  --color-ink-olive: #36421E;
  --color-accent-blue: #3B82F6;
  --color-surface: #FFFFFF;
}
```

- 上記4色の色相は変更しない。
- 既存青系アクセントは青のまま維持し、別の紫・青緑へ置換しない。
- 新しいブランド色を追加しない。
- 成功、注意、危険の既存色は状態用途に限り維持する。

### 5.2 許可する派生

```css
:root {
  --color-lime-soft: rgba(163, 230, 53, 0.12);
  --color-lime-muted: rgba(163, 230, 53, 0.24);
  --color-lime-border: rgba(163, 230, 53, 0.46);
  --color-olive-strong: #36421E;
  --color-olive-muted: rgba(54, 66, 30, 0.68);
  --color-olive-subtle: rgba(54, 66, 30, 0.48);
  --color-olive-border: rgba(54, 66, 30, 0.14);
  --color-olive-divider: rgba(54, 66, 30, 0.09);
  --color-blue-soft: rgba(59, 130, 246, 0.10);
  --color-blue-border: rgba(59, 130, 246, 0.34);
  --color-focus-ring: rgba(59, 130, 246, 0.34);
  --color-scrim: rgba(54, 66, 30, 0.46);
}
```

- 濃淡は透明度で管理し、近似色を無制限に追加しない。
- 本文は `--color-ink-olive`、補足はその透明度派生を使う。
- ライムはブランド、選択、主要進捗に使う。
- 青は案内、診断、リンク、フォーカスに使う。
- 同じ操作内でライムと青を競合させない。
- ライム背景上の文字はオリーブを基本とし、白文字は使用前にコントラストを検証する。

## 6. タイポグラフィ

```css
:root {
  --font-family-body: 'Zen Maru Gothic', 'Noto Sans JP', sans-serif;
  --font-family-heading: 'Zen Kaku Gothic New', 'Noto Sans JP', sans-serif;
  --font-size-12: 0.75rem;
  --font-size-14: 0.875rem;
  --font-size-16: 1rem;
  --font-size-18: 1.125rem;
  --font-size-22: 1.375rem;
  --font-size-28: 1.75rem;
  --font-size-40: 2.5rem;
  --line-tight: 1.25;
  --line-normal: 1.55;
  --line-relaxed: 1.75;
}
```

- 本文の最小基準は16pxとし、補助情報のみ14pxを許可する。
- 12pxはバッジや短いメタ情報だけに使う。
- 見出しは `Zen Kaku Gothic New`、本文は `Zen Maru Gothic` を維持する。
- 数値は可能なら `font-variant-numeric: tabular-nums` を指定する。
- ボタンラベルは原則1行、動詞から始める。
- 長い説明は最大65文字程度の行長に抑える。
- 英単語カードの表面は可変サイズで、長語でも折返しと余白を確保する。

## 7. スペーシングとレイアウト

```css
:root {
  --space-2: 0.125rem;
  --space-4: 0.25rem;
  --space-8: 0.5rem;
  --space-12: 0.75rem;
  --space-16: 1rem;
  --space-24: 1.5rem;
  --space-32: 2rem;
  --space-48: 3rem;
  --space-64: 4rem;
}
```

- 基本単位は4px、主要な配置は8px刻みとする。
- カード内余白はモバイル16px、タブレット以上24pxを基準にする。
- セクション間は32px、セクション内の要素間は12〜16pxとする。
- 生徒画面の本文最大幅は720pxを目安に広げ、学習カードは読みやすい幅を維持する。
- ログインフォームは440pxを上限にする。
- 管理画面はサイドバー280px前後、本文は残り幅を使用する。

## 8. Radius・Elevation・Border

```css
:root {
  --radius-control: 10px;
  --radius-card: 16px;
  --radius-feature: 24px;
  --radius-pill: 999px;
  --shadow-1: 0 1px 2px rgba(54, 66, 30, 0.06);
  --shadow-2: 0 8px 24px rgba(54, 66, 30, 0.09);
  --shadow-3: 0 16px 40px rgba(54, 66, 30, 0.13);
  --border-subtle: 1px solid rgba(54, 66, 30, 0.09);
  --border-default: 1px solid rgba(54, 66, 30, 0.14);
}
```

- 入力・小ボタンは10px、標準カードは16px、主役カードは24pxに限定する。
- 通常カードは境界線と `shadow-1`、主役カードだけ `shadow-2` を使う。
- モーダルのみ `shadow-3` を許可する。
- hoverで影を増やす場合も1段階だけ上げる。
- 同じカードに太い境界、強い影、グラデーションを重ねない。

## 9. アイコン方針

- 操作アイコンは既存の React Icons 系へ統一する。
- サイズは16、20、24pxの3段階とする。
- アイコン単独ボタンは44×44px以上にし、見えるラベルか `aria-label` を付ける。
- 同じ意味には同じアイコンを使用する。
- 矢印は進む・戻る、鉛筆は編集、ゴミ箱は削除に固定する。
- 絵文字は結果画面の祝福など、操作ではない表現に限定する。
- 装飾アイコンは読み上げ対象から外す。

## 10. Motion

```css
:root {
  --motion-fast: 120ms;
  --motion-standard: 200ms;
  --motion-card: 320ms;
  --ease-standard: cubic-bezier(0.2, 0, 0, 1);
}
```

- hover・press・focusは120ms、パネル遷移は200ms、カード反転は320msとする。
- hover浮上は最大2px、pressは `scale(0.98)` 程度に留める。
- 進捗バーは値変更時だけ滑らかに伸ばす。
- ローディングは回転かスケルトンのどちらかに統一する。
- `prefers-reduced-motion: reduce` では、bounce、浮上、回転の中間演出を停止する。
- 動きだけで回答結果を伝えず、文字と状態表示を残す。

## 11. 共通コンポーネント状態

### 11.1 ボタン

- Primary: ライム背景、オリーブ文字、主要行動専用。
- Accent: 青背景または青境界、診断・案内の行動専用。
- Secondary: 白背景、オリーブ境界、補助行動用。
- Ghost: 透明背景、戻る・編集・閉じる用。
- Danger: 既存の赤を削除確認後の実行操作だけに使う。
- Disabled: コントラストを下げ、カーソルと説明で理由を示す。
- Loading: ラベル幅を維持し、スピナーと処理中テキストを表示する。
- Focus-visible: 青の3pxリングを必ず表示する。

### 11.2 入力欄

- ラベルは常時表示し、placeholderだけに役割を持たせない。
- 通常、hover、focus、error、disabled、readonlyを定義する。
- エラー文は入力欄の直下に置き、関連付ける。
- パスワード表示切替は実装する場合も方式自体は変更しない。
- 入力高さは44px以上を確保する。

### 11.3 選択カード・チップ

- 未選択は白背景と薄い境界にする。
- hoverはライムの薄い背景にする。
- 選択中はライム境界、薄いライム背景、チェック、太字を併用する。
- disabledは理由をカード内に表示する。
- `aria-pressed` または適切なradio/checkbox semanticsを付ける。

### 11.4 フィードバック状態

- Loading: 何を取得中か短く表示する。
- Empty: 状態の説明と次の行動を1つ示す。
- Error: 問題、影響、再試行の順に表示する。
- Success: 保存内容と次の行動を表示する。
- Offline: 保存待ちか再試行必要かを区別する。

## 12. 画面別 Before → After

### 12.1 ログイン画面

- Before: ヒーローとカードは存在するが、フォームのまとまりとエラー領域が弱い。
- After: 左に短い価値説明、右に最大440pxのログインカードを置く2カラム構成にする。
- モバイルは価値説明を圧縮し、フォームを先に見せる1カラムにする。
- ロゴ、見出し、説明、フォームの間隔を32/16/24pxで整える。
- 入力ラベルとフィールドを明確に関連付ける。
- ログインボタンを唯一のプライマリーにする。
- エラーはカード上部のインラインバナーにする。
- 背景は `#F7FEE7`、カードは白、ライムとオリーブを中心に維持する。

### 12.2 目標設定画面

- Before: 日付、やる気、複数カテゴリが長く連続し、保存操作が遠い。
- After: 上部に「1.期限 2.ペース 3.目標」のステップ要約を置く。
- 各セクションは標準カード、現在選択中だけ境界を強める。
- やる気カードは名称、所要時間、1日の量を同じ位置に揃える。
- 目標チップは2列を基本とし、狭い画面では1列にする。
- 選択済み目標をフッター直上に短く要約する。
- 保存ボタンは安全領域対応のsticky footerへ置く。
- リセットはゴースト操作に下げる。

### 12.3 生徒ダッシュボード

- Before: サマリー、タスク、告知、タブが近い強さで並ぶ。
- After: 最上部を「今日の学習」主役カードにし、残量、所要時間、開始ボタンを集約する。
- 目標進捗は主役カードの次に、数値とバーを横並びで表示する。
- 今日のタスクは重要度順に並べ、完了済みは彩度ではなくチェックと薄い表示で区別する。
- 単語力未診断の青い案内は、説明を短くしCTAを1つにする。
- おかわり学習は主導線より1段弱いカードにする。
- ヘッダーはロゴ、現在レベル、ユーザーメニューに整理する。
- ログアウトは常時強調せず、ユーザーメニュー内へ寄せる。
- 下部タブは選択中をライム面、アイコン、ラベルで示す。

### 12.4 教材・レベル・自由学習選択

- Before: 選択肢が多く、推奨・対象外・進捗が1枚に混在する。
- After: 上部に選択中教材と推奨理由を固定表示する。
- 絞り込みタブは同じ高さと幅で整える。
- カード内情報を「名前、説明、進捗、状態」の4段に統一する。
- 推奨バッジは1種類に絞り、カード右上へ固定する。
- 対象外はボタンを無効にするだけでなく、理由を1行表示する。
- 親レベルからサブレベルへの遷移はパンくずまたは戻る行で現在地を示す。
- インライン指定の戻るボタンは共通Secondaryへ置換する。

### 12.5 新規学習・復習フラッシュカード

- Before: 固定位置の単語帳ボタン、カード、進捗、複数戻る操作が競合する。
- After: ヘッダーを戻る、モード名、現在数の3要素にする。
- 単語帳切替はヘッダー内のセグメントまたはメニューに収める。
- カードを画面の視覚中心へ置き、表面は単語だけに集中させる。
- 裏面は単語、意味、例文、和訳の順に固定する。
- スワイプ方向はカード下に短い左右ラベルで表示する。
- 前へ戻る操作はフッター左、終了はヘッダーのゴースト操作にする。
- 進捗は細いバーと「12 / 30」を同じ行に置く。
- タップ領域を44px以上にし、キーボード操作も提供する。

### 12.6 単語力チェック

- Before: 適応レベル、早期終了、モード条件など内部ロジックの説明が常時並ぶ。
- After: 常時表示はテスト名、ステージ、回答数、進捗だけにする。
- 推定精度や平均時間は「診断の詳細」へ折りたたむ。
- 初学者・上位層などの内部名は、生徒向けには安心できる短い説明へ翻訳する。
- カードとスワイプ説明はフラッシュカードと同じ構造にする。
- 回答後の短いフィードバックは色、アイコン、テキストを併用する。
- 中断操作は誤タップしにくい位置に置き、確認を挟む。
- ステージ変更時だけ短い区切り画面を表示する。

### 12.7 テスト結果

- Before: レベル、語彙数、達成度、助言がすべて大きなカード内に並ぶ。
- After: 最上部は推定レベルと短い意味説明に集中する。
- 次に語彙数、進捗、回答時間を同じ幅の統計行で並べる。
- 学習アドバイスは優先度1位を開いた状態、残りを折りたたむ。
- 「ダッシュボードに戻る」をPrimaryにする。
- 再テストはSecondaryに下げ、実行条件があれば添える。
- アニメーションは結果数値の出現と進捗伸長だけに絞る。

### 12.8 ストーリー一覧・生成・閲覧

- Before: 生成操作、利用制限、既存作品が別々の強さで表示される。
- After: 今月の生成可否を状態パネルとして最初に示す。
- 生成可能時だけPrimaryを表示し、不可なら次回日付を明記する。
- 作品カードは題名、日付、使用単語数、閲覧操作を固定配置する。
- 本文画面は読みやすい行長、行間、段落間隔を優先する。
- 使用単語のハイライトはライムの薄い背景と下線を併用する。
- 印刷操作は閲覧画面のSecondaryにする。

### 12.9 管理者ダッシュボード

- Before: Bootstrap系グレーと生徒画面のブランド表現が混在する。
- After: 白、淡い黄緑、オリーブ、既存青で同じ製品の管理面に揃える。
- ヘッダーは製品名、画面名、管理者メニューを分ける。
- サイドバー上部に分析、インポート、生徒登録のグローバルナビを置く。
- 生徒検索と一覧をその下の独立領域にする。
- 選択中の生徒はライムの左境界、薄い背景、太字で示す。
- 本文見出しに選択中の生徒名と主要メタ情報を固定する。
- 分析カードの数値位置と高さを揃える。
- 要フォロー対象には理由と「詳細を見る」を同じ行に置く。
- 空状態は白紙にせず、選択または登録の次の行動を示す。

### 12.10 CSVインポート

- Before: ファイル選択、実行、結果が単一カード内で近接する。
- After: 「選択 → 検証 → 差分確認 → 実行 → 結果」のステップ表示にする。
- ファイル名、件数、検証状態をアップロード領域内に表示する。
- 追加、更新、変更なし、削除候補を同じ形式の件数カードで示す。
- 通常更新はPrimary、全置換は危険領域に分離する。
- エラーは行番号、項目、理由、修正例を表形式で表示する。
- 長い一覧は内部スクロールにし、実行操作は画面下部に保持する。

### 12.11 生徒詳細・削除確認

- Before: 復習、ストーリー、学習時間、削除が縦に長く連続する。
- After: 概要、学習、復習、作品のタブまたはセクションナビを使う。
- KPIは3〜4枚に限定し、数値と期間を揃える。
- 最近の学習記録は日付、教材、時間、単語数の列を揃える。
- 削除操作はページ末尾のDanger zoneへ隔離する。
- 確認モーダルには生徒名、削除対象、取消、削除を明記する。
- 初期フォーカスは取消へ置き、危険操作をデフォルトにしない。

## 13. Responsive設計

### 13.1 ブレークポイント

- 0〜479px: small mobile。
- 480〜767px: large mobile。
- 768〜1023px: tablet。
- 1024〜1439px: desktop。
- 1440px以上: wide desktop、本文の最大幅を維持する。

### 13.2 共通ルール

- CSSメディアクエリを使用し、`window.innerWidth` による描画分岐を廃止する。
- 高さは `100dvh` を基本にし、フォールバックとして `100vh` を置く。
- `env(safe-area-inset-bottom)` を下部タブとsticky操作へ加える。
- 2列カードは要素幅が320pxを下回る前に1列へ落とす。
- 横スクロールは表・グラフの限定コンテナだけで許可する。
- モーダルはモバイルで下部シート、768px以上で中央ダイアログにする。
- 管理サイドバーは768px未満でドロワーにする。
- タッチ対象間は8px以上空ける。
- キーボード表示中も送信ボタンとエラーが見えるようにする。

## 14. Accessibility計画

- ページごとに一意な `h1` を置き、見出しレベルを飛ばさない。
- クリック可能な `div` を `button`、`a`、適切な入力要素へ置換する。
- タブは `role="tablist"`、選択状態、対応パネルを関連付ける。
- モーダルは名称、説明、フォーカストラップ、Escape終了を実装する。
- フォーカス順を視覚順と一致させる。
- `:focus-visible` の青リングを全操作要素へ適用する。
- ライム背景と白文字の組合せは避け、オリーブ文字を使う。
- 本文・入力・状態文はWCAG AAのコントラストを受け入れ条件にする。
- グラフと進捗は数値テキストも併記する。
- 色による正誤には「正解」「要復習」などの文字を添える。
- ライブ更新は必要な成功・失敗だけ `aria-live` で通知する。
- 装飾画像・アイコンには空の代替テキストを設定する。
- スワイプ操作にはボタンまたはキーボードの代替手段を用意する。
- 200%拡大時に本文や操作が欠けないことを確認する。
- reduced motion設定で学習を完了できることを確認する。

## 15. 実装ファイル対応

| 対象 | 主な作業 |
|---|---|
| `src/index.css` | reset、body、フォント、基礎フォーカス、dvhを定義 |
| `src/App.css` | トークン、共通部品、生徒画面、学習画面を層別に再編 |
| `src/AdminDashboard.css` | 管理画面固有レイアウトへ限定し、共通定義の重複を削除 |
| `src/Analytics.css` | グラフ、統計、分析カードの密度と数値整列を統一 |
| `src/LoginPage.js` | semantic form、label関連付け、インライン状態領域を整備 |
| `src/GoalSetter.js` | ステップ要約、選択状態、sticky footerの構造を整備 |
| `src/StudentDashboard.js` | インラインスタイルをクラス化し、主役カードとヘッダーを再構成 |
| `src/LearningFlashcard.js` | 学習ヘッダー、カード、進捗、フッター操作を共通構造化 |
| `src/ReviewFlashcard.js` | 新規学習と同じ骨格・状態表現へ統一 |
| `src/VocabularyCheckTest.js` | 診断情報の折りたたみと集中レイアウトを実装 |
| `src/TestResult.js` | 結果階層、統計行、推奨、次アクションを再構成 |
| `src/AdminDashboard.js` | ナビ、一覧、詳細、CSV、Danger zoneを意味構造化 |
| `src/LevelBadge.js` | サイズと状態バリエーションを限定 |
| `src/ProgressLamp.js` | 色以外のラベル・形状による状態表現を追加 |
| `src/PrintableQuiz.js` | 印刷用の白黒可読性、余白、改ページを調整 |
| `src/PrintableStory.js` | 本文行長、単語強調、印刷時の可読性を調整 |

## 16. 実装フェーズ

### Phase D0: UIインベントリとスクリーン基準

- 主要画面を390×844、768×1024、1440×900で記録する。
- ボタン、入力、カード、バッジ、状態表示を一覧化する。
- 既存の直接指定と重複セレクタを分類する。
- 変更前スクリーンショット名と主要導線を固定する。

受け入れ条件:

- 全主要画面に比較用の基準画像がある。
- 重複スタイルとインラインスタイルの移行先が決まっている。

### Phase D1: トークンと共通プリミティブ

- 色、文字、余白、角丸、影、動きのトークンを導入する。
- Button、Input、Card、Badge、Banner、Progressの見た目を統一する。
- focus、hover、active、disabled、loadingを実装する。
- 既存色をトークンへ対応付け、色相が変わらないことを確認する。

受け入れ条件:

- 同じ役割の部品が全画面で同じ状態表現を持つ。
- 変更禁止4色と既存青系アクセントの色相が維持されている。
- CSSに新しい無計画な16進色が追加されていない。

### Phase D2: 認証・目標・生徒ホーム

- ログイン、目標設定、生徒ダッシュボードを改善する。
- 今日の主行動と進捗を最優先にする。
- 下部タブとsticky actionの安全領域を整える。
- エラー、空、読込状態を同じパターンにする。

受け入れ条件:

- 各画面の主行動が1つに見える。
- 390pxで横スクロールや操作隠れがない。
- キーボードだけでログインと目標設定を完了できる。

### Phase D3: 学習・診断・結果

- 新規学習、復習、単語力チェックのシェルを共通化する。
- カードを主役にし、診断内部情報を折りたたむ。
- スワイプ以外の回答手段を提供する。
- 結果画面の情報階層と次アクションを整える。

受け入れ条件:

- 小さい端末でもカード、進捗、主要操作が同時に把握できる。
- reduced motionでもすべての学習操作が成立する。
- 回答・正誤・無効状態を色なしでも判断できる。

### Phase D4: 管理画面

- 管理ナビ、生徒一覧、詳細、分析、CSVを共通設計へ寄せる。
- サイドバーの責任をナビと生徒選択に分ける。
- データ比較、注意対象、破壊的操作の階層を明確にする。
- タブレット以下のドロワーを実装する。

受け入れ条件:

- 選択中の画面と生徒が常に判別できる。
- CSVの各段階と差分件数が視覚的に追える。
- 削除操作が通常導線から分離されている。
- 1440pxで情報が間延びせず、768pxでも操作可能である。

### Phase D5: アクセシビリティ・Motion・最終統合

- semantic、フォーカス、コントラスト、読み上げを検証する。
- motion tokenとreduced motionを全画面へ適用する。
- 印刷画面、長文、長い名前、ゼロ件、大量件数を確認する。
- 不要な旧CSSと上書きを削除する。

受け入れ条件:

- 主要導線をキーボードのみで完了できる。
- WCAG AA相当のコントラストを満たす。
- 200%拡大で文字・操作が欠けない。
- 主要画面のvisual regression差分が意図どおりである。

## 17. Visual QAチェックリスト

### 共通

- [ ] ロゴ、ページ名、主操作の視線順が自然である。
- [ ] 見出し、本文、補足の3階層が判別できる。
- [ ] カードの角丸と影が定義した段階に収まる。
- [ ] ライム、淡い黄緑、オリーブ、青の色相が変わっていない。
- [ ] 新しい色が主要操作へ混入していない。
- [ ] hover時にレイアウトがずれない。
- [ ] focus ringが切れず、背景に埋もれない。
- [ ] disabled状態が有効状態と区別できる。
- [ ] loading時にボタン幅が変わらない。
- [ ] error文が対象操作の近くにある。

### 390×844

- [ ] 横スクロールが発生しない。
- [ ] 下部タブとsticky actionが重ならない。
- [ ] 主要ボタンが44px以上ある。
- [ ] 長い英単語と日本語名が欠けない。
- [ ] ソフトウェアキーボード表示時もフォーム送信できる。
- [ ] 学習カードが最初の画面内で十分に見える。

### 768×1024

- [ ] 1列から2列への切替が不自然でない。
- [ ] 管理サイドバーまたはドロワーが操作できる。
- [ ] モーダルの高さが画面を超えない。
- [ ] 統計カードの高さが揃っている。

### 1440×900

- [ ] 本文幅が広がりすぎず、視線移動が短い。
- [ ] 管理画面の一覧と詳細の比率が適切である。
- [ ] ログインカードが横に伸びすぎない。
- [ ] 空白が過剰でなく、情報密度が一定である。

### 状態・データ差

- [ ] 初回ユーザー、学習途中、全タスク完了を確認する。
- [ ] 目標未設定、期限切れ、データ0件を確認する。
- [ ] 非常に長い名前、タイトル、例文を確認する。
- [ ] 通信中、失敗、再試行、保存成功を確認する。
- [ ] 生徒0人、1人、100人相当の管理一覧を確認する。
- [ ] CSV正常、警告あり、エラー多数を確認する。
- [ ] ストーリー生成可能、処理中、今月生成済みを確認する。

## 18. `IMPLEMENTATION_PLAN.md` との順序関係

本書のデザイン作業は、機能実装を妨げないよう次の順序で差し込む。

| 機能計画 | デザイン計画の扱い |
|---|---|
| フェーズ0 作業保全 | D0の画面基準とUIインベントリだけ実施 |
| フェーズ1 公開・ルーティング | 共通Loading/Error/Not Foundの最小スタイルを適用 |
| フェーズ2 CSV・データ保全 | 機能フロー確定後にD4のCSV画面を実装 |
| フェーズ3 目標統一 | 重複画面削除後にD2の目標画面を実装 |
| フェーズ4 単語ID | UIの全面変更はせず、表示確認だけ行う |
| フェーズ5 日次計画 | 表示量確定後にD2の今日の学習カードを実装 |
| フェーズ6 単語力チェック | ロジック確定後にD3を実装 |
| フェーズ7 AIストーリー | 保存先統一後にストーリー状態を実装 |
| フェーズ8 UI・安定性 | D1〜D5を統合し、旧CSSを整理 |
| フェーズ8 Vite移行 | UI構造変更と別コミットで実施 |
| フェーズ9 最終検証 | Visual QAとアクセシビリティをリリースゲート化 |

### 順序上の必須ルール

1. 重複した目標画面を削除してから目標UIを磨く。
2. 単語力チェックのロジックを安定させてから表示情報を削る。
3. CSV差分仕様を確定してからステップUIを組む。
4. ストーリー保存先を統一してから一覧の空・済・失敗状態を作る。
5. 共通トークンは早期導入するが、旧CSS削除は各画面移行後に行う。
6. Vite移行と大規模CSS整理は同一コミットにしない。
7. 色相固定は全フェーズのレビュー条件とする。

## 19. 推奨コミット単位

1. `style: add fixed brand tokens and UI primitives`
2. `style: polish login and goal-setting flow`
3. `style: clarify student dashboard hierarchy`
4. `style: unify learning and review shells`
5. `style: simplify vocabulary check presentation`
6. `style: polish result and story states`
7. `style: restructure admin navigation and details`
8. `style: add responsive and reduced-motion coverage`
9. `style: remove migrated legacy and inline styles`
10. `test: add visual and accessibility regression coverage`

## 20. デザイン完了条件

- 現在のライム `#A3E635`、淡い黄緑 `#F7FEE7`、濃いオリーブ `#36421E` の色相が維持されている。
- 既存青系アクセントの色相が維持され、別色相のブランドアクセントが増えていない。
- 全主要画面でプライマリー操作が一意に見える。
- 同じ役割のボタン、カード、入力、バッジ、状態表示が同じ見た目を持つ。
- 生徒はダッシュボードを開いて3秒以内に今日の学習開始場所を見つけられる。
- 学習中はカード、現在数、進捗、回答操作が他の情報より目立つ。
- 管理者は選択中の画面と生徒を常に判別できる。
- 390px、768px、1440pxで横はみ出し、重なり、切れがない。
- 全主要操作がキーボードとタッチの両方で完了できる。
- focus、hover、active、disabled、loading、error、empty、successが定義されている。
- 色だけに依存する状態表現がない。
- reduced motionと200%拡大で機能を完了できる。
- 既存機能、学習結果、保存データ、パスワード方式に回帰がない。
- `IMPLEMENTATION_PLAN.md` の全リリースゲートとVisual QAの両方を通過している。

---

この計画の狙いは、つくたんらしい色を塗り替えることではなく、同じ色が最も効果的に働く階層・余白・状態・動きを全画面で揃えることである。
