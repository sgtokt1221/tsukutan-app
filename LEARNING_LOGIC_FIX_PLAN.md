# つくたん 学習ロジック修正計画

作成日: 2026-08-09  
対象: 実力テスト、新規学習、復習単語、日次学習計画、進捗、学習ログ

## 1. この文書の目的

現在の実装では、画面上は学習を完了できても、次の問題によってデータの意味が崩れる可能性がある。

- 実力テストを1問で中断しても正式結果として保存される。
- 新規学習を完了しても習得語彙数が増えない。
- 初見単語の正解と不正解が同じ復習状態になる。
- 復習完了時に、習得済みであることを示す履歴まで削除される。
- 戻る、連打、通信失敗によって復習間隔が二重更新または欠落する。
- 日次計画が再生成されるたびに単語が入れ替わる。
- 学習ログの形式が分析側の期待と一致していない。

この文書は、個別の症状を場当たり的に直すのではなく、今後の学習機能が同じ基準で動くための修正方針、実装順序、テスト、完了条件を定義する。

## 2. 最重要の設計原則

### 2.1 習得履歴と復習予定を分離する

単語を習得済みかどうかは、復習待ちかどうかとは別の情報である。

- 習得済み単語は復習完了後も削除しない。
- 復習完了は `status: mastered` への変更として扱う。
- `nextReviewAt` が未来なら復習画面に出さないだけで、履歴は残す。
- 新規単語の抽出は、永続的な習得履歴を参照する。

### 2.2 1回答を1回だけ反映する

すべての回答には一意な `eventId` を付け、同じ回答を再送しても進捗を二重更新しない。

- 回答イベントと単語進捗を同じトランザクションで保存する。
- 書き込み中は回答ボタンとスワイプをロックする。
- 二重タップ、戻って再回答、通信再試行でも反復回数を二重加算しない。
- 書き込み失敗は呼び出し元へ例外を返し、成功扱いにしない。

### 2.3 実力推定値と実際の学習履歴を混ぜない

実力テストの語彙数は統計的な推定値であり、個別単語の習得履歴ではない。

- `assessedVocabulary`: 実力テストによる推定語彙数
- `assessedLevel`: 実力テストによる推定レベル
- `learnedAfterAssessmentCount`: 判定後に新しく習得したユニーク単語数
- `currentVocabulary`: 上記を一貫した規則で合成した表示値

判定済みレベル以下の単語を通常の「新規単語」として出して二重加算しない。判定中に不正解だった単語は、弱点復習として個別に管理する。

### 2.4 日次計画をその日一日は固定する

日次計画は画面を開くたびにランダム再生成しない。

- `dailyPlans/{YYYY-MM-DD}` に当日の単語IDと進行状況を保存する。
- 途中退出、再読み込み、別端末ログインでも同じ計画を再開する。
- 再生成は目標変更、明示的な再作成、計画バージョン変更時だけ行う。
- 1日の新規語数は、途中退出を挟んでも計画上限を超えない。

### 2.5 保存成功後にだけ完了扱いにする

- 最後の回答保存を待ってから日次完了を記録する。
- 回答、進捗、ログのどれかが失敗した場合は完了画面へ進まない。
- エラーを `catch` してログ出力だけで握りつぶさない。
- 再試行しても同じ `eventId` を使い、重複更新を防ぐ。

## 3. データモデルの正本

### 3.1 単語進捗

推奨パス:

```text
users/{uid}/wordProgress/{wordId}
```

推奨フィールド:

```json
{
  "wordId": "persistent-word-id",
  "status": "learning | reviewing | mastered",
  "firstSeenAt": "Timestamp",
  "learnedAt": "Timestamp | null",
  "lastAnsweredAt": "Timestamp",
  "lastReviewedAt": "Timestamp | null",
  "nextReviewAt": "Timestamp | null",
  "lastResult": "correct | incorrect",
  "repetitions": 0,
  "lapses": 0,
  "intervalDays": 0,
  "easeFactor": 2.5,
  "source": "placement | daily-new | daily-review | free-study | adjacent",
  "updatedAt": "Timestamp"
}
```

重要な制約:

- `mastered` になっても文書を削除しない。
- 単語本文はマスターを正本とし、進捗文書には原則としてIDと学習状態だけを保存する。
- レガシー互換のため単語本文を残す場合も、判定や重複排除は必ず永続IDで行う。
- `repetitions`、`intervalDays`、`easeFactor` は欠損・不正値を正規化してから計算する。

### 3.2 回答イベント

推奨パス:

```text
users/{uid}/studyEvents/{eventId}
```

推奨フィールド:

```json
{
  "eventId": "sessionId:wordId:attemptNumber",
  "sessionId": "session-id",
  "sessionType": "placement | new | review | free",
  "wordId": "persistent-word-id",
  "questionLevel": 4,
  "result": "correct | incorrect",
  "revealedBeforeAnswer": false,
  "responseTimeMs": 1800,
  "answeredAt": "Timestamp"
}
```

分析処理はこの形式だけを参照し、`action`、`correct`、`isCorrect` などの別名を混在させない。

### 3.3 日次計画

推奨パス:

```text
users/{uid}/dailyPlans/{YYYY-MM-DD}
```

推奨フィールド:

```json
{
  "dateKey": "2026-08-09",
  "timeZone": "Asia/Tokyo",
  "planVersion": 2,
  "generatedAt": "Timestamp",
  "newWordIds": [],
  "reviewSessions": [[]],
  "adjacentWordIds": [],
  "answeredNewWordIds": [],
  "completedReviewSessionIndexes": [],
  "newWordsCompleted": false,
  "completedAt": null
}
```

## 4. 実力テストの修正計画

### 4.1 途中終了

- 途中終了では `users.level`、`assessedVocabulary`、`currentVocabulary` を更新しない。
- 「結果を破棄して戻る」と「テストを続ける」を表示する。
- 再開機能を付ける場合は、正式結果とは別のドラフトへ回答履歴を保存する。
- 最低回答数未満の結果は正式判定として保存できない。

### 4.2 開始レベル

- 初回はLv.3から開始する。
- 再テストは前回の `assessedLevel` を開始レベルにする。
- 開始レベルが1〜7外の場合だけLv.3へ戻す。

### 4.3 出題の偏りをなくす

現在の「目標レベル±1から一括抽選」は、レベル別の収録語数に引っ張られる。目標Lv.5では、候補の約61%をLv.6が占める。

修正方針:

- 問題数を実レベル別に割り当ててから抽選する。
- 例: 10問なら `target-1: 2問 / target: 6問 / target+1: 2問`。
- 上限・下限レベルでは余った枠を目標レベルへ戻す。
- 回答履歴には `targetLevel` と `questionLevel` の両方を保存する。
- 最終判定は実際の `questionLevel` 別正答率を使う。

### 4.4 答えを見た回答を区別する

- 答えを見る前に回答した場合は `recalled` とする。
- 答えを見た後の「知っていた」は `recognizedAfterReveal` として低い評価にする。
- 最も単純にする場合は、実力テストでは回答するまで意味を表示しない。

### 4.5 取り消しと復習登録

- テスト中は回答と不正解単語をローカル状態に保持する。
- 「前の問題」で回答を取り消した場合、永続データへ副作用を残さない。
- テスト完了後に、最終回答が不正解だったユニーク単語だけを復習へ登録する。

### 4.6 結果表示

- 最大レベル表示を `/ 7` に統一する。
- 保存した `assessedVocabulary` を結果画面へ渡し、その値を表示する。
- `levels.json.wordsRequired` を表示する場合は「レベルの目安語彙数」と明記する。
- 「推定語彙数」という同じ名称で異なる数値を表示しない。

## 5. 新規学習の修正計画

### 5.1 初回正解と初回不正解を分ける

初めて進捗文書を作るときも、回答結果を適用する。

初回正解の例:

```json
{
  "status": "reviewing",
  "lastResult": "correct",
  "repetitions": 1,
  "intervalDays": 1,
  "nextReviewAt": "翌日の日本時間基準"
}
```

初回不正解の例:

```json
{
  "status": "learning",
  "lastResult": "incorrect",
  "repetitions": 0,
  "lapses": 1,
  "intervalDays": 0,
  "nextReviewAt": "当日"
}
```

### 5.2 習得語数

- 画面のインデックス数を、そのまま習得語数として加算しない。
- `wordProgress` に初めて習得状態が作られたユニークIDだけを数える。
- 同じ単語を自由学習、日次学習、復習で何度回答しても一度しか加算しない。
- 実力テストの推定範囲と重複する単語は二重加算しない。

### 5.3 不正解単語の二重登録をやめる

- `LearningFlashcard` と `StudentDashboard` の両方から復習登録しない。
- 回答更新を一つのサービスへ集約する。
- `onBack(incorrectWords)` を使った再登録処理は削除する。
- 既存の復習間隔を初期値へ上書きしない。

### 5.4 戻る操作

次のどちらかに統一する。

1. 推奨: 前の単語は閲覧だけ可能とし、再回答はできない。
2. 回答をセッション終了時までローカル保持し、戻った場合は未保存の回答を置き換える。

回答直後に保存したうえで無制限に再回答させる現在の方式は廃止する。

## 6. 復習ロジックの修正計画

### 6.1 復習完了

- 文書削除ではなく `status: mastered` へ変更する。
- `nextReviewAt` は長期確認日または `null` にする。
- 苦手へ戻った場合は同じ文書を `reviewing` に戻す。
- 習得済みIDとしては常に残す。

### 6.2 復習間隔

- やる気設定は、必要なら今回の間隔倍率にだけ適用する。
- `easeFactorMultiplier` を毎回答Ease Factorへ乗算しない。
- Ease Factorは最低値と妥当な上限を持たせる。
- `intervalDays`、`repetitions`、`easeFactor` の欠損値を正規化する。
- 日付は `Asia/Tokyo` の日付キーを基準に計算する。

推奨テスト系列:

- 初回正解
- 連続正解2回、3回、5回、10回
- 正解後の不正解
- 不正解直後の正解
- low / normal / high
- Ease Factor欠損、interval欠損、古いTimestamp
- 日本時間23:59と翌日00:00

### 6.3 セッション分割

- `reviewSessions` を実際の画面入力に使用する。
- 1セッション上限は30語とする。
- 完了後に次セッションを始めるか選べるようにする。
- 大量の復習語を1画面へ一括投入しない。

## 7. 単語帳モードの修正計画

- DOM位置からカードを推測せず、操作対象の `wordId` をハンドラーへ直接渡す。
- 正解、不正解、復習完了をフラッシュカードと同じ回答サービスへ接続する。
- PCとスマートフォンで同じ判定関数を使う。
- TODOのまま画面だけ反応する操作を残さない。
- 上スワイプで復習完了したカードはセッション配列からも取り除く。
- 削除後に同じカードへ回答して再登録されないようにする。
- 進捗を復元するなら保存処理も実装する。保存しない場合は復元コードを削除する。
- `wordbookProgress` とフラッシュカードの `currentIndex` を別々の正本として持たない。

## 8. 日次計画の修正計画

### 8.1 新規・復習・隣接語を分ける

- `newWords`: 初めて学ぶ単語
- `reviewWords`: 既に学習履歴があり、復習期日を迎えた単語
- `adjacentWords`: 未学習だが先取り表示する単語
- `masteredCheckWords`: 習得済みの長期確認単語

未学習の隣接語を「復習」として処理しない。回答した場合は新規学習として習得履歴を作る。

### 8.2 固定と再開

- 初回生成時に単語IDを日次計画へ保存する。
- 再読み込みでは保存済み計画を返す。
- 回答済みIDを保存し、未回答位置から再開する。
- 完了済み計画を再生成しない。
- 目標変更時は新しい `planVersion` で明示的に再生成する。

### 8.3 完了条件

- 新規学習の全回答が保存済みである。
- 日次計画の回答済みIDが予定IDを満たしている。
- 最後の回答保存と日次完了更新が成功している。
- 復習の完了状況は新規学習とは別に保持する。

## 9. ログと分析の修正計画

### 9.1 ログ形式を一つにする

正式フィールド:

- `sessionType`: `placement | new | review | free`
- `result`: `correct | incorrect`
- `wordId`
- `eventId`
- `durationMs`
- `answeredAt`
- `revealedBeforeAnswer`

廃止または移行する混在フィールド:

- `correct`
- `isCorrect`
- `action: correct/incorrect`
- `duration` と `durationInSeconds` の混在
- `graduatedCount` と `wordsGraduated` の混在

### 9.2 分析処理

- 学習ペースは `sessionType: new` のユニーク `wordId` を日別に数える。
- 苦手判定は最新回答または一定期間の正誤履歴から作る。
- 「正解数」と「復習完了数」を分ける。
- 学習ログ保存失敗時は、分析だけ欠けた状態を黙って作らない。

## 10. 実装順序

### フェーズ0: 仕様固定と安全網

1. この文書のデータ定義を確定する。
2. 現在のFirestoreデータをバックアップする。
3. 重要な再現テストを先に追加する。
4. 旧ロジックを通る箇所を一覧化する。

### フェーズ1: 回答保存基盤

1. `applyWordAnswer` を新設する。
2. 回答イベントと単語進捗をトランザクション保存する。
3. `eventId` による重複防止を実装する。
4. エラーを呼び出し元へ返す。
5. 新規学習と復習の直接Firestore書き込みを置き換える。

### フェーズ2: 習得履歴と復習

1. `wordProgress` を新しい正本にする。
2. 復習完了を削除から状態変更へ変える。
3. 初回正解・不正解を正しく保存する。
4. 戻る・連打・再試行を安全にする。
5. 復習間隔と日本時間を統一する。

### フェーズ3: 新規学習と語彙数

1. `newlyLearnedCount` の画面依存を廃止する。
2. ユニークな新規習得IDから語彙数を更新する。
3. 親画面からの不正解単語再追加を削除する。
4. 実力推定値と実学習数を分離する。

### フェーズ4: 日次計画

1. 日次計画をFirestoreへ保存する。
2. 途中再開を実装する。
3. 隣接語を復習から分離する。
4. 30語単位の復習セッションを実際に使う。
5. 完了判定を保存成功後へ移動する。

### フェーズ5: 実力テスト

1. 途中終了時の正式保存を停止する。
2. 再テストの開始レベルを前回結果にする。
3. 実レベル別の層化抽出へ変更する。
4. `questionLevel` を回答履歴へ保存する。
5. 答え表示前後を区別する。
6. 結果画面のレベル・語彙数を保存値と一致させる。

### フェーズ6: 単語帳・ログ・分析

1. 単語帳操作を `wordId` ベースに作り直す。
2. ログスキーマを統一する。
3. ペース分析と苦手分析を新ログへ接続する。
4. 旧フィールドの読み取り互換を期間限定で残す。

### フェーズ7: 移行と公開

1. 既存 `reviewWords` を `wordProgress` へ移行する。
2. 反復回数、間隔、Ease Factor、次回復習日を保持する。
3. `progress.currentVocabulary` を `assessedVocabulary` へ退避する。
4. ドライランでユーザー別件数と欠損を確認する。
5. 段階公開し、保存失敗率と重複イベント数を監視する。

## 11. 必須テスト

### 11.1 実力テスト

- 0問で退出しても保存されない。
- 1問正解・1問不正解で退出してもレベルが変わらない。
- 前の問題へ戻って回答を変えても副作用が残らない。
- 再テストは前回レベルから始まる。
- レベル別の出題数が設計比率どおりになる。
- 実際の問題レベルが回答履歴へ残る。
- 保存語彙数と結果画面の表示が一致する。

### 11.2 新規学習

- 初回正解と初回不正解で異なる状態になる。
- 20語完了でユニークな習得語数が正しく増える。
- 同じ20語を再度学習しても語彙数は増えない。
- 最後の単語が不正解でも取りこぼさない。
- 途中退出と再読み込みで同じ計画を再開する。
- 書き込み失敗時は日次完了にならない。

### 11.3 復習

- 正解で次回復習日が進む。
- 不正解で当日復習へ戻る。
- 復習完了後も習得履歴が残る。
- 復習完了した単語が新規単語へ戻らない。
- 前の単語を見ても反復回数が二重加算されない。
- 二重タップでも1回答だけ保存される。
- 同じ回答を再送しても結果が一度しか反映されない。
- 30語を超える復習が複数セッションに分かれる。

### 11.4 単語帳

- 操作したカードの `wordId` が更新される。
- PCとスマートフォンで正解・不正解・復習完了が動く。
- 復習完了したカードが同じセッションで再追加されない。
- 保存した進捗だけが復元される。

### 11.5 ログ・時間

- 新規学習ログが `sessionType: new` になる。
- 正誤が `result` に統一される。
- 学習ペースがユニーク単語IDから計算される。
- 日本時間の日付境界で復習予定がずれない。

## 12. 完了条件

次をすべて満たしたときに修正完了とする。

- 途中終了した実力テストが正式結果を上書きしない。
- 新規学習後にユニーク習得語数と残り語彙数が更新される。
- 初回正解と初回不正解が異なる復習予定になる。
- 復習完了後も習得済みIDが永続的に残る。
- 戻る、連打、再試行で回答が二重反映されない。
- 日次計画を再読み込みしても単語と進捗が変わらない。
- 復習セッションが30語単位で動く。
- 単語帳モードの全操作が実データへ反映される。
- 保存失敗を成功として扱わない。
- 学習ログと分析処理が同じスキーマを使う。
- 実力テスト結果画面と保存値が一致する。
- 新規・復習・実力テストの主要経路にコンポーネントテストとFirestore統合テストがある。

## 13. 触る主なファイル

```text
src/logic/reviewLogic.js
src/logic/learningPlanner.js
src/logic/studyLogger.js
src/logic/knowledgeAnalysis.js
src/logic/basicAnalytics.js
src/logic/placementTestEngine.js
src/LearningFlashcard.js
src/ReviewFlashcard.js
src/VocabularyCheckTest.js
src/TestResult.js
src/StudentDashboard.js
src/components/learning/AnswerControls.js
```

新設候補:

```text
src/logic/wordProgressService.js
src/logic/studyEventService.js
src/logic/dailyPlanRepository.js
src/logic/reviewScheduler.js
scripts/migrate-word-progress.js
```

この修正では、画面ごとに個別の保存処理を持たせず、回答・進捗・計画・ログの正本をロジック層へ集約することが最も重要である。
