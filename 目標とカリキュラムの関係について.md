# 目標とカリキュラムの関係について

## 📋 システムの全体像

**つくたん**では、目標設定とカリキュラムが密接に関連しており、以下の流れで動作しています：

## 1. 📝 目標設定プロセス

### 目標設定画面（`GoalSetter.js`）
- **目標の種類**：英検、高校入試、大学入試の3カテゴリ
- **複数選択可能**：複数の目標を同時に設定可能
- **達成日設定**：目標達成までの期限を設定
- **やる気レベル**：学習のペースを決定（「そこそこ」「普通」「やる気満々」）

### マスターデータ（`setupMasterData.js`）
```javascript
// 例：英検2級
{ id: 'eiken_2', data: { 
  displayName: '英検2級 合格', 
  requiredVocabulary: 5100, 
  description: '高校卒業レベル。社会で求められる英語力のスタンダードです。' 
}}

// 例：高校入試（偏差値60）
{ id: 'hs_60', data: { 
  displayName: '高校入試（偏差値60）合格', 
  requiredVocabulary: 3000, 
  description: '難関公立・中堅私立高校レベル。応用的な長文にも対応できます。' 
}}
```

## 2. 🧠 カリキュラム生成ロジック

### 学習計画生成（`learningPlanner.js`）

#### A. やる気レベル別設定
```javascript
const MOTIVATION_LEVELS = {
  low: {    // 「そこそこ」
    name: 'そこそこ',
    description: '無理せず続けたい',
    dailyReviewQuota: 5,      // 復習単語数
    newWordsQuota: 15,        // 新規単語数
    intervalMultiplier: 1.5,  // 復習間隔を長く
    easeFactorMultiplier: 1.2,
    masteredThreshold: 5
  },
  normal: { // 「普通」
    name: '普通',
    description: 'バランスよく学習したい',
    dailyReviewQuota: 10,
    newWordsQuota: 20,
    intervalMultiplier: 1.0,
    easeFactorMultiplier: 1.0,
    masteredThreshold: 6
  },
  high: {   // 「やる気満々」
    name: 'やる気満々',
    description: '短期間で集中して取り組みたい',
    dailyReviewQuota: 15,
    newWordsQuota: 30,
    intervalMultiplier: 0.8,  // 復習間隔を短く
    easeFactorMultiplier: 0.9,
    masteredThreshold: 7
  }
}
```

#### B. 語彙数推定（`vocabularyEstimator.js`）
- 目標の最大語彙数を取得
- 現在の語彙力（実力テスト結果）を考慮
- 不足語彙数を計算

```javascript
export const estimateNeededWords = async (userData) => {
  // 複数目標から最大の語彙数を取得
  let maxRequiredVocabulary = 0;
  for (const target of userData.goal.targets) {
    const goalDoc = await getDoc(doc(db, 'goalsMaster', target.goalId));
    const requiredVocab = goalDoc.data().requiredVocabulary;
    if (requiredVocab > maxRequiredVocabulary) {
      maxRequiredVocabulary = requiredVocab;
    }
  }
  
  // 現在の語彙力から不足分を計算
  const currentVocabulary = userData.progress?.currentVocabulary || 0;
  const neededWords = Math.max(0, maxRequiredVocabulary - currentVocabulary);
  
  return neededWords;
};
```

#### C. 日次学習計画の生成
```javascript
export const generateDailyPlan = async (userData, userId) => {
  const neededWordsCount = await estimateNeededWords(userData);
  const targetDateStr = userData.goal?.targetDate;
  const motivationLevel = userData.goal?.motivationLevel || 'normal';
  
  // やる気レベルに応じた学習量を決定
  const config = MOTIVATION_LEVELS[motivationLevel];
  
  // 残り日数と単語数を計算
  const today = new Date();
  const targetDate = new Date(targetDateStr);
  const remainingDays = Math.ceil((targetDate - today) / (1000 * 60 * 60 * 24));
  
  return {
    newWords: [],           // 新規学習単語
    reviewWords: [],        // 復習単語
    extraNewWords: [],      // 追加新規単語
    dailyTarget: Math.ceil(neededWordsCount / remainingDays), // 1日の目標単語数
    remainingDays: remainingDays,       // 残り日数
    remainingWords: neededWordsCount    // 残り単語数
  };
}
```

## 3. 🎯 推奨システム

### 教材推奨（`StudentDashboard.js`）
- **実力テスト結果**に基づいて推奨教材を決定
- **目標との整合性**をチェック
- **大阪府公立入試英単語**は高校受験目標がある場合のみ推奨

```javascript
const isRecommendedTextbook = (textbookId, testLevel, userData) => {
  // 大阪府公立入試英単語の場合は高校受験目標がある場合のみ推奨
  if (textbookId === 'osaka-koukou-nyuushi') {
    const isHighSchoolTarget = isHighSchoolExamTarget(userData);
    if (!isHighSchoolTarget) return false;
  }
  
  // レベルマッピングで推奨判定
  const textbookLevelMapping = {
    'osaka-koukou-nyuushi': { min: 1, max: 10 }, // 全レベル対応（高校受験目標がある場合のみ）
    'highschool-english': { min: 4, max: 7 },    // 高校レベル
    'eiken-5': { min: 1, max: 2 },
    'eiken-4': { min: 2, max: 3 },
    'eiken-3': { min: 3, max: 4 },
    'eiken-pre2': { min: 4, max: 5 },
    'eiken-2': { min: 5, max: 6 },
    'eiken-pre1': { min: 6, max: 7 },
    'eiken-1': { min: 7, max: 8 }
  };
  
  const mapping = textbookLevelMapping[textbookId];
  return testLevel >= mapping.min && testLevel <= mapping.max;
};
```

### レベル推奨
```javascript
const getRecommendedLevels = (testLevel) => {
  const recommendations = [];
  
  // 現在レベルより1つ下から2つ上までを推奨
  for (let level = Math.max(1, testLevel - 1); level <= Math.min(10, testLevel + 2); level++) {
    const priority = level === testLevel ? 'high' : 
                    level === testLevel + 1 ? 'medium' : 'low';
    recommendations.push({ level, priority });
  }
  
  return { recommended: recommendations, current: testLevel };
};
```

## 4. 📊 学習進捗管理

### 復習システム（`reviewLogic.js`）
- **SM-2アルゴリズム**による間隔反復学習
- **やる気レベル**に応じた復習間隔の調整
- **習熟度**に基づく単語の管理

```javascript
export const updateUserWordProgress = async (userId, word, isCorrect, isReviewComplete = false, motivationLevel = 'normal') => {
  const config = MOTIVATION_LEVELS[motivationLevel];
  
  if (isCorrect) {
    // 正解の場合：復習間隔を延長
    newInterval = Math.round(interval * easeFactor * config.intervalMultiplier);
    newEaseFactor = Math.max(1.3, easeFactor + 0.1);
    newRepetitions = repetitions + 1;
  } else {
    // 不正解の場合：復習間隔をリセット
    newInterval = 1;
    newEaseFactor = Math.max(1.3, easeFactor - 0.2);
    newRepetitions = 0;
  }
  
  // やる気レベルに応じた習得基準
  const isMastered = newRepetitions >= config.masteredThreshold;
  
  if (isMastered) {
    // 習得完了：復習リストから除去
    await removeWordFromReview(userId, word.id);
  }
};
```

## 5. 🔄 データフロー

```
目標設定 → 語彙数推定 → やる気レベル適用 → 日次学習計画生成 → 推奨教材表示 → 学習実行 → 進捗更新 → 復習スケジュール調整 → 日次学習計画生成
```

### 詳細なフロー
1. **目標設定**：ユーザーが目標と達成日、やる気レベルを設定
2. **語彙数推定**：目標の語彙数と現在の語彙力の差を計算
3. **学習計画生成**：やる気レベルに応じて日次の学習量を決定
4. **推奨システム**：実力テスト結果と目標に基づいて教材を推奨
5. **学習実行**：推奨された教材で学習を実施
6. **進捗管理**：学習結果に基づいて復習スケジュールを調整
7. **適応的調整**：学習状況に応じて計画を動的に調整

## 6. 🎨 特徴的な仕組み

### 1. 目標の複数選択対応
- 英検2級 + 高校入試など複数目標を同時設定可能
- 複数目標の最大語彙数を基準に学習計画を生成

### 2. やる気レベルによる調整
- **「そこそこ」**：無理のないペースで継続重視
- **「普通」**：バランスの取れた学習
- **「やる気満々」**：集中的で密度の高い学習

### 3. 適応的推奨システム
- 実力テスト結果と目標に基づく教材推奨
- 大阪府公立入試英単語は高校受験目標がある場合のみ推奨
- レベル別の細かな推奨判定

### 4. SM-2アルゴリズム
- 科学的な間隔反復学習による効率的な記憶定着
- やる気レベルに応じた復習間隔の調整
- 習熟度に基づく自動的な復習リスト管理

### 5. リアルタイム適応
- 学習進捗に応じた動的な計画調整
- 習得済み単語の自動的な復習リストからの除去
- 隣接レベル単語の追加学習

## 7. 📁 関連ファイル

- **`GoalSetter.js`**: 目標設定画面
- **`learningPlanner.js`**: 学習計画生成ロジック
- **`vocabularyEstimator.js`**: 語彙数推定
- **`reviewLogic.js`**: 復習システム
- **`StudentDashboard.js`**: 推奨システム
- **`setupMasterData.js`**: 目標マスターデータ
- **`knowledgeAnalysis.js`**: 知識分析
- **`progressLogic.js`**: 進捗管理

## 8. 🎯 まとめ

このシステムでは、**目標設定 → カリキュラム生成 → 学習実行 → 進捗管理**の流れで、個人に最適化された学習計画が自動的に作成・調整されています。

特に重要なのは：
- **やる気レベル**による学習ペースの個人最適化
- **実力テスト結果**に基づく適切な教材推奨
- **SM-2アルゴリズム**による科学的な復習システム
- **目標の複数選択**による柔軟な学習計画

これにより、ユーザーは自分の目標と現在の実力に最適化された、効率的で継続しやすい学習体験を得ることができます。

