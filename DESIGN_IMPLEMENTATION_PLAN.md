# つくたん デザイン実装計画

作成日: 2026-08-09  
対象: 会話内で承認した「生徒ホーム・学習カード・先生画面」のモック、および新しい「つくたん」ロゴ  
状態: 実装着手前の統合計画

関連文書:

- [IMPLEMENTATION_PLAN.md](./IMPLEMENTATION_PLAN.md): 機能、データ、公開の全体計画
- [DESIGN_POLISH_PLAN.md](./DESIGN_POLISH_PLAN.md): デザイン原則と全画面の改善方針
- [TSUKUTAN_LOGO_PROMPT.json](./TSUKUTAN_LOGO_PROMPT.json): ロゴ生成に使用したデザイン仕様
- [public/tsukutan-logo-lockup-v1.png](./public/tsukutan-logo-lockup-v1.png): 今回採用するロゴ画像

![つくたん ロゴ](./public/tsukutan-logo-lockup-v1.png)

---

## 1. この計画の目的

先に作成したモックの方向性を、現在のReactアプリへ安全に実装する。

完成時は次の3画面を製品の中心として統一する。

1. 生徒ホーム: 今日やることが3秒以内に分かる。
2. 学習カード: 単語と回答操作だけに集中できる。
3. 先生画面: 対応が必要な生徒を短時間で見つけられる。

新しいロゴは単なる画像差し替えではなく、ログインから生徒画面、学習画面、管理画面までを同じ製品として結び付けるブランド要素として使う。

### プロダクト仮説

> 新しいロゴと一貫した画面構造を組み合わせることで、生徒には「今日の一歩」が分かり、先生には「次に見るべき生徒」が分かる、親しみやすく信頼できる学習アプリになる。

### 成功の判断

- 生徒ホームを見た利用者が、3秒以内に主要ボタンを指せる。
- 学習中に、スワイプを知らなくてもボタンだけで完走できる。
- 先生が、学習停止中または要確認の生徒を30秒以内に見つけられる。
- ログイン、生徒、学習、管理の各画面で同じロゴ、色、文字、角丸、操作規則が使われる。
- 390px、768px、1440pxの各幅で主要情報が欠けない。
- 既存の学習ロジック、保存処理、認証方式を壊さない。

---

## 2. 実装対象と対象外

### 2.1 今回の実装対象

- 新ロゴのアプリ内導入
- ロゴ表示用の共通コンポーネント
- ログイン画面のブランド統合
- 生徒向け共通シェル
- モックに沿った生徒ホームの再構成
- モックに沿った学習カードの再構成
- モックに沿った先生画面の再構成
- 下部ナビゲーションと管理サイドバーの統一
- 読込中、空、エラー、完了状態の共通化
- レスポンシブ、キーボード、読み上げ対応
- スクリーンショットによる視覚QA

### 2.2 今回は変更しないもの

- パスワード方式とFirebase Authenticationの仕様
- Firestoreのデータ構造
- 日次学習計画の計算方法
- 正解・不正解、復習、定着率の判定ロジック
- 語彙力チェックの出題ロジック
- CSVインポートのサーバー側仕様
- ストーリー生成のサーバー側仕様
- ロゴのコンセプト、文字、色相の再デザイン

### 2.3 実装上の大原則

- 表示の変更と学習ロジックの変更を同じ差分に混ぜない。
- `StudentDashboard.js` と `AdminDashboard.js` を一度に全面書き換えない。
- 既存処理を小さな表示コンポーネントへ渡す形で移行する。
- 新しい色相を追加しない。
- モック内の数値を固定値として実装しない。
- 実データがない項目は、無理に作らず非表示または空状態にする。
- 画面ごとの独自ボタンや独自カードを増やさない。

---

## 3. デザインの完成像

### 3.1 全体の印象

- 明るく親しみやすい。
- 幼児向けには寄りすぎない。
- 文字、余白、情報順序で格好よく見せる。
- ライムは主要行動と進捗に限定する。
- 白いカードは必要な情報のまとまりだけに使う。
- 強い影や色数ではなく、階層と整列で品質を出す。

### 3.2 維持する色

既存の `src/styles/tokens.css` を唯一の色定義として使う。

| 役割 | トークン | 色 |
|---|---|---:|
| ブランド | `--color-brand-lime` | `#A3E635` |
| 背景 | `--color-canvas-lime` | `#F7FEE7` |
| 主文字 | `--color-ink-olive` | `#36421E` |
| 補助情報 | `--color-accent-blue` | `#3B82F6` |
| カード | `--color-surface` | `#FFFFFF` |

状態色は成功、注意、エラーの意味にのみ使用する。

### 3.3 モックから採用する構造

#### 生徒ホーム

```text
ブランドヘッダー
  ├─ ロゴ
  └─ 継続日数または短い状態

挨拶
  ├─ 日付
  ├─ 名前
  └─ 一文の案内

今日の主タスク
  ├─ 状態
  ├─ 新規・復習を合わせた残り数
  ├─ 所要時間の目安
  ├─ 進捗
  └─ 学習開始 / 続きから

今週の要約
  └─ 実データがある指標を最大3件

次にできること
  ├─ 復習
  └─ ストーリー

下部ナビ
  ├─ ホーム
  ├─ 自由学習
  ├─ ストーリー
  └─ きろく
```

#### 学習カード

```text
学習ヘッダー
  ├─ 戻る
  ├─ セッション名
  └─ 一時停止または終了

セッション進捗

単語カード
  ├─ レベル
  ├─ 単語
  ├─ 発音
  ├─ 意味
  └─ 例文

回答操作
  ├─ もう一度
  ├─ 迷った
  └─ わかった
```

#### 先生画面

```text
管理シェル
  ├─ ロゴ付きサイドバー
  └─ メイン領域

ページヘッダー
  ├─ クラス名と期間
  └─ 主要操作

概要指標
  ├─ 在籍人数
  ├─ 学習率
  └─ 要確認人数

生徒一覧
  └─ 名前、今週の学習、状態

生徒詳細
  ├─ レベル
  ├─ 最重要の所見
  ├─ 直近推移
  └─ 詳細への導線
```

---

## 4. ロゴ導入計画

### 4.1 採用するロゴ

元画像:

- ファイル: `public/tsukutan-logo-lockup-v1.png`
- サイズ: 2172 × 724px
- 比率: 3:1
- 内容: 左に「若葉＋開いた単語帳」のシンボル、右に「つくたん」
- 背景: 淡い黄緑
- 文字色: 濃いオリーブ
- シンボル: ブランドライム、白、濃いオリーブ

このファイルは削除や上書きをせず、v1の原本として保持する。

### 4.2 ロゴの役割

- ログイン画面ではブランドの主役として使う。
- 生徒ホームではアプリの現在地を示すヘッダーとして使う。
- 学習画面では小さく使い、単語カードより目立たせない。
- 管理画面ではサイドバー上部に置き、生徒画面との一貫性を作る。
- カード内部、結果演出、ボタン内には繰り返し置かない。

### 4.3 表示サイズ

| 場所 | モバイル | PC | 備考 |
|---|---:|---:|---|
| ログイン | 幅220px前後 | 幅280px前後 | ページの主役 |
| 生徒ヘッダー | 幅126〜144px | 幅150px前後 | 高さを44〜50px以内にする |
| 学習ヘッダー | 幅108〜120px | 幅120〜132px | 主課題より弱くする |
| 管理サイドバー | 幅144〜164px | 幅160〜180px | サイドバー幅内に収める |

実装では固定ピクセルだけでなく `clamp()` と `max-width: 100%` を併用する。

### 4.4 `BrandLogo` コンポーネント

追加候補:

```text
src/components/brand/BrandLogo.js
src/components/brand/BrandLogo.css
```

想定API:

```jsx
<BrandLogo
  placement="login | student-header | study-header | admin-sidebar"
  linked={true | false}
  priority={true | false}
/>
```

実装要件:

- 画像パスを一箇所に集約する。
- `width` と `height` 属性を指定し、読込時のレイアウトずれを防ぐ。
- ログインの最初の画像は遅延読み込みしない。
- ページ下部など重要度が低い場合のみ遅延読み込みを使う。
- ホームへのリンクの場合は、リンクの読み上げ名を「つくたん ホーム」にする。
- ロゴだけがブランド名を表す場合は `alt="つくたん"` とする。
- 隣に同じ文字がある場合は `alt=""` とし、重複読み上げを避ける。
- 画像の中の文字だけにページ見出しの意味を持たせない。
- ログインには画面上の `h1` を残し、必要なら視覚的に隠す。

### 4.5 背景の扱い

現行PNGには淡い黄緑の背景が含まれるため、次のルールを守る。

- ロゴの周囲は `--color-canvas-lime` にする。
- 白いカードへ画像を直接置かない。
- 管理サイドバーが白の場合は、ロゴ領域だけ淡い黄緑のブランドブロックにする。
- `mix-blend-mode` やフィルターで無理に背景を消さない。
- CSSで色相、明度、彩度を変更しない。

### 4.6 派生アセット

初回の画面実装では横長PNGをそのまま使う。

その後、同じ見た目を維持したまま次を用意する。

- `tsukutan-logo-lockup-v1.webp`: 通信量削減用
- `tsukutan-logo-mark-v1.png`: 左側シンボル単体
- `logo192.png`: PWAアイコン用の承認済み書き出し
- `logo512.png`: PWAアイコン用の承認済み書き出し
- 将来の `tsukutan-logo-lockup.svg`: 印刷と高密度表示向けの手動トレース版

注意:

- 生成PNGは「ベクター風」であり、実際のベクターデータではない。
- faviconとPWAアイコンの差し替えは、シンボル単体の縮小確認後に行う。
- 派生アセット作成時に形状や文字を再解釈しない。

---

## 5. 共通コンポーネント構成

推奨構成:

```text
src/
  components/
    brand/
      BrandLogo.js
      BrandLogo.css
    layout/
      StudentShell.js
      StudentHeader.js
      StudentBottomNav.js
      StudyShell.js
      AdminShell.js
      PageHeader.js
    ui/
      Button.js
      IconButton.js
      Surface.js
      ProgressBar.js
      StatusBadge.js
      MetricCard.js
      FeedbackBanner.js
      EmptyState.js
      LoadingState.js
    student/
      TodayTaskCard.js
      WeeklySummary.js
      NextActionList.js
    learning/
      SessionHeader.js
      WordCard.js
      AnswerControls.js
      AudioControl.js
    admin/
      AdminMetricGrid.js
      StudentList.js
      StudentListItem.js
      StudentDetailPanel.js
      AdminToolbar.js
  styles/
    tokens.css
    primitives.css
    layout.css
```

### 5.1 既存CSSとの関係

- `src/styles/tokens.css` を継続利用する。
- `src/styles/primitives.css` の既存ボタン状態を拡張する。
- `App.css` は画面固有レイアウト中心へ縮小する。
- `AdminDashboard.css` は管理画面固有の表・グリッド中心へ縮小する。
- 新規コンポーネントの共通状態を画面CSSへ重複定義しない。
- インラインスタイルは進捗幅などの動的数値に限定する。

### 5.2 コンポーネント化の優先順位

1. `BrandLogo`
2. `Button` / `IconButton`
3. `ProgressBar` / `StatusBadge`
4. `StudentShell` / `StudyShell` / `AdminShell`
5. `TodayTaskCard`
6. `WordCard` / `AnswerControls`
7. `StudentList` / `StudentDetailPanel`
8. Empty / Loading / Error状態

### 5.3 状態の必須セット

各操作コンポーネントは次を持つ。

- default
- hover
- active
- focus-visible
- disabled
- loading
- success
- error

選択要素は `aria-pressed`、現在地は `aria-current` を使う。

---

## 6. 生徒ホーム実装

### 6.1 対象ファイル

- `src/StudentDashboard.js`
- `src/App.css`
- 新規の `src/components/student/*`
- 新規の `src/components/layout/StudentShell.js`
- 新規の `src/components/layout/StudentBottomNav.js`

### 6.2 現在の課題

- レベル、再テスト、学習ペース、やる気、締切、今日のタスクが同じ強さで並ぶ。
- 主タスクより先に多数の情報を読む必要がある。
- `div` のクリックで開始するタスクカードがあり、キーボード操作に弱い。
- インラインスタイルとイベントによる直接装飾が多い。
- 下部ナビのラベルと選択状態が統一されていない。

### 6.3 画面構成

#### ファーストビュー

1. `StudentHeader`
2. 日付、名前、短い案内
3. `TodayTaskCard`
4. 主要ボタン

レベル、締切計算、やる気、ペース詳細は主タスクより下へ移す。

#### `TodayTaskCard`

表示内容:

- 未着手、学習中、完了の状態
- 今日の新規語数
- 今日の復習語数
- 合計残数
- 完了数 / 全体数
- 所要時間の目安
- 「学習を始める」または「学習をつづける」

主ボタンの行動:

- 新規語が残る: 新規学習を開始
- 新規語が完了し復習が残る: 復習を開始
- 両方完了: おかわりまたは次の任意行動
- データ読込失敗: 再読込

#### 実データ対応

| モック表示 | 現在のデータ候補 |
|---|---|
| 新規語数 | `dailyPlan.newWords.length` |
| 復習語数 | `dailyPlan.reviewWords.length` |
| 完了状態 | `isDailyTaskCompleted` と日次記録 |
| 総進捗 | `progressPercentage` |
| 次の案内 | `outstandingSummary` |
| 目標期限 | `scheduleMetrics` |
| 学習ペース | `paceSuggestion` |
| レベル | `testResultLevel` |

データが存在しない継続日数や週次指標を固定値で表示しない。正式な取得元が確定するまでは、その枠自体を出さない。

### 6.4 週次要約

- 初期表示は最大3項目。
- 実データがある指標だけを出す。
- 詳細分析への入口を一つ付ける。
- 数値の良し悪しだけでなく短い説明を付ける。
- 読込中は同じ高さのスケルトンを使う。

### 6.5 次にできること

- 「復習する」は復習対象がある場合だけ強調する。
- ストーリーは当月の生成状態に応じて文言を変える。
- 自由学習は下部ナビを主導線とし、重複カードを増やさない。

### 6.6 下部ナビ

ラベル:

- ホーム
- 自由学習
- ストーリー
- きろく

要件:

- アイコンと日本語ラベルを併記する。
- 現在地に `aria-current="page"` を付ける。
- 最低44pxのタップ領域を確保する。
- iPhoneのセーフエリアを考慮する。
- 本文末尾にナビ高さ分の余白を確保する。
- `HOME` と日本語表記の混在をなくす。

### 6.7 完了条件

- 初回、途中、完了、データエラーの4状態がある。
- 主要ボタンが最初の表示範囲にある。
- タスクカードが `button` またはリンクとして操作できる。
- 画面内のprimary actionは原則一つ。
- ロゴが背景と自然につながる。
- 390pxで横スクロールしない。

---

## 7. 学習カード実装

### 7.1 対象ファイル

- `src/LearningFlashcard.js`
- `src/ReviewFlashcard.js`
- `src/App.css`
- 新規の `src/components/learning/*`
- 新規の `src/components/layout/StudyShell.js`

### 7.2 現在の課題

- スワイプ、ダブルクリック、長押しなど操作方法が多い。
- 回答操作が初見で分かりにくい。
- 動的な色とインラインスタイルが多い。
- 学習モードと単語帳モードの表示責任が一つのファイルに集中している。
- デバッグログが画面実装と近い。

### 7.3 移行方針

学習ロジックを変えず、次の順で表示を分離する。

1. 現在単語と進捗をview modelにまとめる。
2. `SessionHeader` を抽出する。
3. `WordCard` を抽出する。
4. `AnswerControls` を抽出する。
5. 音声操作を `AudioControl` へ分離する。
6. スワイプをボタン操作の補助へ変更する。
7. 復習画面も同じ部品へ移行する。

### 7.4 回答モデル

画面上のボタン:

- もう一度
- 迷った
- わかった

既存の保存ロジックへ接続する際は、意味の対応表を先に確定する。

| 新UI | 既存処理 | 保存結果 |
|---|---|---|
| わかった | 正解処理 | 習熟度を上げる |
| 迷った | 要復習処理 | 近い復習対象にする |
| もう一度 | 不正解処理 | セッション内または復習へ戻す |

「迷った」に対応する既存状態がない場合は、UIだけ先に追加しない。まず保存仕様を `IMPLEMENTATION_PLAN.md` 側で決める。

### 7.5 ジェスチャー

- 見える回答ボタンを正式な操作とする。
- スワイプは同じ処理を呼ぶショートカットにする。
- ダブルクリック必須を廃止する。
- 長押し必須を廃止する。
- ドラッグ方向と回答意味を画面内で一度説明する。
- 誤操作を防ぐため、縦スクロールと横スワイプの閾値を分ける。

### 7.6 カード表示

- 単語を最上位にする。
- 発音ボタンを単語の近くに置く。
- 意味と例文の表示タイミングを統一する。
- 長い語、複数行の意味、長い例文でも高さを固定しすぎない。
- カードの切替で周囲のレイアウトを跳ねさせない。
- 回答後の色演出は既存色の範囲にする。
- 動きを減らす設定では回転と移動を止める。

### 7.7 進捗

- 現在数 / 全体数を常時表示する。
- `progressbar` の読み上げ値を設定する。
- 完了直前と完了後の状態を明確に分ける。
- セッション終了前に保留中の保存処理を待つ。

### 7.8 完了条件

- タッチ、マウス、キーボードだけで完走できる。
- スワイプを使わなくても同じ結果になる。
- 学習と復習の骨格が同じである。
- 音声失敗時に学習を継続できる。
- 保存中の二重操作を防止する。
- 文字量による重なりがない。
- 390pxと1440pxでカードの読みやすい幅が保たれる。

---

## 8. 先生画面実装

### 8.1 対象ファイル

- `src/AdminDashboard.js`
- `src/AdminDashboard.css`
- 新規の `src/components/admin/*`
- 新規の `src/components/layout/AdminShell.js`

### 8.2 現在の課題

- 生徒一覧、分析、CSV、登録、詳細が一つの大きなコンポーネントに集中している。
- サイドバーが生徒一覧と管理ナビを同時に担っている。
- 一覧から要確認理由を判断しにくい。
- クリック可能な `div` があり、キーボードで選択しにくい。
- グラフやカードに固有の色、角丸、影が残っている。

### 8.3 管理シェル

PC:

- 左に約240〜280pxの固定幅サイドバー。
- 右に最大1200pxのメイン領域。
- サイドバー上部にロゴ領域。
- ナビ、学校・クラス情報、ログアウトを縦に整理する。

モバイル:

- サイドバーをヘッダーとドロワーへ変える。
- 生徒一覧は重要列だけのカードまたは短い行表示にする。
- 生徒詳細は一覧の下または別画面へ移す。

### 8.4 画面の情報順序

1. クラス名、期間、主要操作
2. 在籍、学習率、要確認の概要
3. 要確認の生徒
4. 全生徒一覧
5. 選択した生徒の詳細
6. 詳細分析、CSV、印刷などの補助機能

### 8.5 生徒一覧

初期列:

- 生徒名
- 今週の学習日数または達成率
- 状態

必要に応じて表示する列:

- 現在レベル
- 最終学習日
- 復習残数

要件:

- 行全体を `button` またはリンクとして操作可能にする。
- 選択状態を背景、境界、読み上げ状態で示す。
- 状態を色だけで表さない。
- 100人規模でも高さと密度を保つ。
- 生徒IDなど補助情報は主情報より弱くする。

### 8.6 生徒詳細

最初に表示するもの:

- 生徒名
- レベル
- 現在の要約
- 次に確認すべきこと
- 直近3期間の推移

詳細へ送るもの:

- 全学習履歴
- 過去のストーリー
- 印刷クイズ作成
- 詳細な学習時間
- 削除などの危険な操作

削除操作は通常の詳細パネルから視覚的に離す。

### 8.7 CSV・生徒登録

- モックの主画面では入口だけを整理する。
- CSVの機能実装は `IMPLEMENTATION_PLAN.md` のフェーズ2を優先する。
- 生徒登録は共通モーダルへ移行する。
- 成功とエラーをフォーム内に表示する。
- 現在の初期パスワード方針は今回変更しない。

### 8.8 完了条件

- 先生が要確認の生徒を30秒以内に見つけられる。
- 一覧をキーボードで移動・選択できる。
- 100人規模でも画面全体が過度に長くならない。
- 390pxで主要操作へ到達できる。
- 管理ロゴ領域の背景が画像背景と自然につながる。
- CSV、生徒登録、分析の入口が競合しない。

---

## 9. ログイン画面実装

### 9.1 対象ファイル

- `src/LoginPage.js`
- `src/App.css`
- `src/components/brand/BrandLogo.js`

### 9.2 変更内容

- 現在の文字だけの `hero-title` を新ロゴへ置き換える。
- ページの `h1` として「つくたん」をHTML上に残す。
- `TSUKUTAN` の英字副題はロゴ仕様に合わせて表示から外す。
- キャッチコピーはロゴの下に一文だけ残す。
- フォーム幅を最大440pxへ制限する。
- ロゴ領域とフォームの間隔を32px前後に統一する。
- エラーはフォーム内で表示する。
- ログイン中はボタンの文言と無効状態を維持する。

### 9.3 完了条件

- ロゴ画像の文字とHTML上のブランド名が読み上げで重複しない。
- 画像の読込前後でフォーム位置が大きく動かない。
- Enterキーでログインできる。
- エラー後も入力値が残る。
- 390pxでロゴ、入力欄、ボタンが無理なく見える。

---

## 10. その他画面への展開

中心3画面が完成した後、同じシェルと部品を次へ展開する。

### 優先順

1. 目標設定
2. 語彙力チェック
3. テスト結果
4. 自由学習の教材・レベル選択
5. ストーリー一覧・閲覧
6. 詳細分析
7. CSV取り込み
8. 印刷画面

### 展開ルール

- 各画面にロゴを追加しすぎない。
- アプリシェル内に既にロゴがあれば、本文には再表示しない。
- 主ボタンは一つ。
- 進捗、状態、カード、見出しを共通部品へ置き換える。
- 画面ごとの新しい色や角丸を作らない。

---

## 11. 実装フェーズ

### Phase UI-0: 基準状態の保存

#### 作業

- 現在の主要画面を390px、768px、1440pxで保存する。
- ログイン、生徒ホーム、学習、復習、管理の状態一覧を作る。
- `StudentDashboard.js` と `AdminDashboard.js` の表示・ロジック境界を記録する。
- ロゴ原本のサイズ、ハッシュ、表示背景を記録する。
- 既存のテストを実行して基準結果を残す。

#### 完了条件

- 改善前後を比較できる。
- 既存の失敗と新規の失敗を区別できる。
- ロゴ原本が誤って上書きされない。

### Phase UI-1: ロゴとブランド部品

#### 作業

- `BrandLogo` を追加する。
- ログインへロゴを導入する。
- 生徒・学習・管理シェルにロゴ配置領域を作る。
- 画像寸法、alt、リンク、読込優先度を設定する。
- ロゴ背景と各画面背景を合わせる。

#### 完了条件

- 4種類の配置で同じコンポーネントを使う。
- 画像比率が崩れない。
- 背景の矩形が不自然に見えない。
- 読み上げが重複しない。

### Phase UI-2: 共通プリミティブとシェル

#### 作業

- Button、IconButton、ProgressBar、StatusBadgeを整理する。
- StudentShell、StudyShell、AdminShellを追加する。
- PageHeader、LoadingState、EmptyState、FeedbackBannerを統一する。
- 共通フォーカスと無効状態を追加する。
- インラインのhover変更をCSSへ移す。

#### 完了条件

- 主要部品の状態一覧を確認できる。
- 色、余白、角丸、影がトークンを参照する。
- 新しい生の色値が画面CSSに増えていない。

### Phase UI-3: 生徒ホーム

#### 作業

- `StudentDashboard.js` から今日の表示用データをまとめる。
- `TodayTaskCard` を追加する。
- 今日のタスクをファーストビューへ移す。
- 週次要約を最大3件にする。
- 復習・ストーリーの次アクションを整理する。
- 下部ナビを日本語ラベル＋アイコンへ変更する。
- `div onClick` を意味のある操作要素へ変更する。

#### 完了条件

- 初回、途中、完了、エラーの4状態を確認済み。
- 3秒テストを通過する。
- 既存の日次計画と保存結果が変わらない。

### Phase UI-4: 学習カード

#### 作業

- SessionHeader、WordCard、AnswerControlsを抽出する。
- 見える回答ボタンを追加する。
- 学習と復習で同じ骨格を使う。
- 発音、意味、例文の順序を統一する。
- スワイプを補助操作へ下げる。
- 進捗と保存中状態を明示する。

#### 完了条件

- ボタンだけで学習を完走できる。
- 既存の習熟度更新と学習ログが一致する。
- 中断と完了で保留中の書込みを待つ。

### Phase UI-5: 先生画面

#### 作業

- AdminShellを導入する。
- 管理ナビと生徒一覧を分離する。
- 概要指標を3件に整理する。
- 生徒一覧を操作可能な行へ変更する。
- 生徒詳細パネルを抽出する。
- 要確認理由を文章で表示する。
- モバイルをドロワーまたは縦積みにする。

#### 完了条件

- 100人規模の表示確認済み。
- キーボードで一覧と詳細を操作できる。
- 既存のCSV、分析、生徒登録へ到達できる。

### Phase UI-6: 残り画面の統合

#### 作業

- 目標設定、テスト、結果、自由学習、ストーリーへ共通部品を展開する。
- 画面ごとの古いボタン、カード、見出しを削除する。
- エラー、空、読込中を統一する。
- 印刷時にアプリシェルとロゴ表示を適切に制御する。

#### 完了条件

- 主要導線に旧UIと新UIの混在がない。
- ロゴが必要な場所に一度だけ表示される。

### Phase UI-7: レスポンシブとアクセシビリティ

#### 作業

- 390px、768px、1440pxで全主要画面を確認する。
- 200%文字拡大を確認する。
- キーボード操作とフォーカス順を確認する。
- 読み上げ名、進捗値、現在地を確認する。
- `prefers-reduced-motion` を確認する。
- コントラストを確認する。

#### 完了条件

- 意図しない横スクロールがない。
- 固定ナビが本文を隠さない。
- 色だけで状態を伝える箇所がない。
- 主要操作のタップ領域が44px以上ある。

### Phase UI-8: 視覚QAと公開

#### 作業

- 基準画像との比較を行う。
- ロゴ、文字、余白、角丸、影を画面横断で確認する。
- 実データで長い名前、0件、100件、エラーを確認する。
- build、lint、testを実行する。
- ステージング環境で主要導線を完走する。
- 公開後のロールバック単位を確認する。

#### 完了条件

- 視覚差分が承認される。
- 機能テストと画面テストが通る。
- 公開候補版で生徒・先生の主要タスクを完走できる。

---

## 12. `IMPLEMENTATION_PLAN.md` との実行順

| 本計画 | 全体実装計画との関係 |
|---|---|
| UI-0 | 全体フェーズ0と並行可能 |
| UI-1 | 独立して着手可能 |
| UI-2 | 全体フェーズ1のルーティング確定後 |
| UI-3 | 日次計画と進捗データが安定した時点 |
| UI-4 | 復習・保存・単語ID仕様が安定した時点 |
| UI-5 | CSVと管理APIの画面契約が確定した時点 |
| UI-6 | 各機能フェーズの完了後に順次実施 |
| UI-7 | 全体フェーズ8と統合 |
| UI-8 | 全体フェーズ9と同じ候補版で実施 |

### 並行してよい作業

- ロゴ部品の追加
- CSSトークンとプリミティブの整理
- 静的なStudentShell、StudyShell、AdminShellの追加
- スクリーンショット基準の作成
- コンポーネント単体テスト

### 待つべき作業

- 「迷った」の保存意味を決めずに回答UIを接続すること
- 日次計画のデータ確定前に進捗表示を固定すること
- CSV API確定前に管理画面の完了状態を固めること
- シンボル単体を確認せずPWAアイコンを置き換えること

---

## 13. テスト計画

### 13.1 コンポーネントテスト

#### `BrandLogo`

- placementごとのクラスが付く。
- `width` と `height` がある。
- リンクあり・なしが正しく切り替わる。
- altの重複を防げる。

#### `TodayTaskCard`

- 未着手で「学習を始める」。
- 途中で「学習をつづける」。
- 新規完了・復習残りで復習を開始する。
- 全完了で完了状態になる。
- 0件でも壊れない。

#### `AnswerControls`

- 各ボタンが対応処理を一度だけ呼ぶ。
- 保存中は二重押下できない。
- キーボードで操作できる。
- 無効状態が読み上げられる。

#### `StudentList`

- 選択中の生徒を示す。
- 空一覧を表示できる。
- 長い名前を表示できる。
- 100人分を表示できる。

### 13.2 統合テスト

- ログイン後、生徒ホームにロゴと今日のタスクが表示される。
- 今日のタスクから新規学習を開始できる。
- 回答後に進捗と保存データが更新される。
- 復習へ移行できる。
- 完了後、生徒ホームの状態が更新される。
- 管理画面で同じ生徒の状態を確認できる。
- ログアウト後、ログイン画面へ戻る。

### 13.3 視覚テスト行列

| 画面 | 390 × 844 | 768 × 1024 | 1440 × 900 | 必須状態 |
|---|---:|---:|---:|---|
| ログイン | 必須 | 任意 | 必須 | 通常、入力中、エラー |
| 生徒ホーム | 必須 | 必須 | 必須 | 初回、途中、完了、エラー |
| 学習カード | 必須 | 必須 | 必須 | 表示、回答中、音声失敗、完了 |
| 復習カード | 必須 | 必須 | 必須 | 通常、0件、完了 |
| 先生画面 | 必須 | 必須 | 必須 | 空、通常、100人、要確認 |
| 生徒詳細 | 必須 | 必須 | 必須 | 選択前、選択後、エラー |

### 13.4 品質コマンド

実装時はリポジトリの正式なコマンドを使う。

```bash
npm test -- --watchAll=false
npm run lint
npm run build
```

UI移行中も、既存ロジックのテストを省略しない。

---

## 14. アクセシビリティ受け入れ基準

- [ ] ロゴ画像の代替テキストが配置に応じて適切である。
- [ ] 画像内文字だけにページ名の意味を依存していない。
- [ ] 主要操作へTabで到達できる。
- [ ] フォーカスの視認性がある。
- [ ] クリック可能な `div` が残っていない。
- [ ] 下部ナビの現在地が読み上げられる。
- [ ] 進捗に現在値、最小値、最大値がある。
- [ ] 回答を色だけで区別していない。
- [ ] タップ領域が44 × 44px以上ある。
- [ ] 200%拡大で主要操作が隠れない。
- [ ] 動きを減らす設定でカードの回転・移動が止まる。
- [ ] モーダルを閉じた後、元の操作へフォーカスが戻る。

---

## 15. パフォーマンス計画

- ロゴには寸法を指定してCLSを防ぐ。
- ログインと初期ヘッダーのロゴは優先読み込みする。
- 同じロゴ画像を複数回ネットワーク取得しない。
- フラッシュカードの切替でコンポーネント全体を再生成しすぎない。
- 管理一覧の計算値を必要に応じてメモ化する。
- 100人を超える場合は一覧のページ分割または仮想化を検討する。
- 装飾目的のアニメーションライブラリ利用を増やさない。
- 派生WebPは視覚確認後に採用する。

---

## 16. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| ロゴPNGの背景が白画面で矩形に見える | ブランド品質が下がる | ロゴ領域を淡い黄緑に統一する |
| 画像内文字だけでタイトルを表す | 読み上げとSEOに弱い | HTMLの見出しを残す |
| 生徒ホームの再構成で学習開始条件が変わる | 誤ったタスクが開始される | view modelと既存handlerの対応表をテストする |
| 回答を3段階化すると既存保存仕様とずれる | 習熟度データが不整合になる | 「迷った」の保存仕様確定まで接続しない |
| 巨大コンポーネントを一括分割する | 回帰が見つけにくい | 画面単位、部品単位で小さく移行する |
| 旧CSSと新CSSが競合する | 画面幅ごとに表示が変わる | 移行済み画面から旧セレクタを削除する |
| モックの固定数値を実装してしまう | 実データと矛盾する | 取得元がない指標は表示しない |
| PWAアイコンを早く置き換える | 小サイズで潰れる | シンボル単体の16/32/192/512px確認後に行う |

---

## 17. 推奨コミット単位

1. `feat(brand): add reusable tsukutan logo component`
2. `refactor(ui): consolidate shared primitives and focus states`
3. `feat(layout): add student study and admin shells`
4. `feat(student): rebuild today-first home hierarchy`
5. `feat(student): unify bottom navigation`
6. `feat(learning): extract word card and answer controls`
7. `feat(learning): make gestures optional`
8. `feat(admin): introduce admin shell and summary metrics`
9. `feat(admin): extract student list and detail panel`
10. `fix(a11y): complete keyboard and screen-reader states`
11. `test(ui): add responsive and visual regression coverage`

ロゴ導入、シェル変更、画面再構成を一つの巨大コミットにしない。

---

## 18. 実装着手順

最初に着手する順序:

1. 現在のスクリーンショットとテスト結果を保存する。
2. `BrandLogo` を追加する。
3. ログイン画面へロゴを導入する。
4. 生徒・学習・管理のシェルを追加する。
5. 生徒ホームの表示用データを整理する。
6. `TodayTaskCard` を実装する。
7. 下部ナビを置き換える。
8. 学習カードの表示だけを抽出する。
9. 回答ボタンを既存処理へ接続する。
10. 管理シェルと生徒一覧を置き換える。
11. 残り画面へ共通部品を展開する。
12. 視覚QA、アクセシビリティ、公開確認を行う。

最初の成果物は「新ロゴが入ったログイン画面」とする。次に「今日のタスクが主役になった生徒ホーム」までを一つの確認可能な区切りとする。

---

## 19. Definition of Done

### ブランド

- [ ] 新ロゴがログイン、生徒、学習、管理に適切な強さで配置されている。
- [ ] ロゴの比率、色、文字が変形していない。
- [ ] ロゴ背景が画面背景と自然につながる。
- [ ] ロゴを過剰に繰り返していない。
- [ ] PWAアイコンは縮小検証後にのみ更新されている。

### 生徒ホーム

- [ ] 今日のタスクがファーストビューの主役である。
- [ ] 状態に応じて開始先が正しい。
- [ ] 固定値の架空指標がない。
- [ ] 下部ナビが日本語とアイコンで統一されている。
- [ ] 初回、途中、完了、エラーが確認済みである。

### 学習

- [ ] 見える回答ボタンだけで完走できる。
- [ ] ジェスチャーは任意である。
- [ ] 学習と復習の構造が統一されている。
- [ ] 音声失敗や保存中でも状態が分かる。
- [ ] 既存の学習記録と習熟度更新が保たれている。

### 先生画面

- [ ] 要確認の生徒が最初に分かる。
- [ ] 生徒一覧と管理ナビの役割が分かれている。
- [ ] 一覧と詳細をキーボード操作できる。
- [ ] 100人規模で確認済みである。
- [ ] CSV、分析、生徒登録の既存機能へ到達できる。

### 品質

- [ ] 390px、768px、1440pxの視覚確認が完了している。
- [ ] build、lint、testが通っている。
- [ ] 主要画面に新しい色相が追加されていない。
- [ ] 主要操作のタップ領域が44px以上ある。
- [ ] 200%文字拡大と動きを減らす設定を確認している。
- [ ] 生徒と先生の主要導線を公開候補版で完走できる。

この条件を満たした時点で、先に作成したモックの方向性と新しいロゴが、見た目だけでなく実際の操作とデータを含めて「つくたん」に統合されたものとする。
